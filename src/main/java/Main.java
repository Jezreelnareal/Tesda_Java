import shared.WebApplication;

/** Compatibility entry point for existing scripts and IDE run configurations. */
public class Main {
    public static void main(String[] args) { WebApplication.main(args); }
}
